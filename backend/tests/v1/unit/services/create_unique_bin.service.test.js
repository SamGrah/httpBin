require('jest');
const { createNewBin, binIdPresentInDb } = require('../../../../src/api/v1/data/index.data');
const { createUniqueBin } = require('../../../../src/api/v1/services/index.service');

describe('Create a new bin', () => {
  test('createUniqueBin() --> create new bin without duplicate', () => {
    
  });
});
