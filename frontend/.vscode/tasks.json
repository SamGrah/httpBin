{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "npm",
			"script": "build",
			"group": "build",
			"problemMatcher": [],
			"label": "npm: build",
			"detail": "vite build"
		},
    {
      "label": "build: api-gateway",
      "type": "shell",
      "command": "docker-compose up -d --no-deps --build api-gateway",
    }
	]
}
